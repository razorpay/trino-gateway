[trino-rest]
    appEnv              = "default"
    serviceName         = "trino-rest"
    hostname            = "stage-trino.rest.razorpay.com"
    port                = 8000
    metricsPort         = 8080
    shutdownTimeout     = 2
    shutdownDelay       = 2
    maxRecords          = 5000
    [trino-backend-db]
        dialect             = "trino"
        protocol            = "tcp"
        url                 = "stage-trino-rest.com"
        port                = 8080
        username            = "user"
        password            = "user"
        catalog             = "default"
        schema              = "public"
        DSN                 = "http://user@stage-trino-rest.com:8080?catalog=default&schema=public"
